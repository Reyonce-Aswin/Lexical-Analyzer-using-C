# Lexical-Analyzer-using-C
This project implements a lightweight and modular Lexer (Tokenizer) written in ANSI C. The lexer converts raw input text into a sequence of tokens for use in compilers, interpreters, or static analysis tools.
